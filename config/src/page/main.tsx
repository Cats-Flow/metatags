import React from 'react'
import ReactDOM from 'react-dom/client'
import { Home } from './home'

ReactDOM.createRoot(document.getElementById('flow')!).render(
  <React.StrictMode>
    <Home />
  </React.StrictMode>,
)
